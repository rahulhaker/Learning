'''
GIT Commands

git status
git log

git init
git add . [-A for All, . New and Modified but not deleted, -U Modified and Deleted not new]
git commit -m "Version1"

git --version
git config --globaluser user.name
git config --globaluser user.email "rahulhaker.gmail.com"

git push origin master
git pull origin master

git diff <Filename>
git diff --Staged <FileName>

git reset <filename> - Moved back from stage to normal any changes.
git checkout <filename> - Clear any changes not in staged and made upto date to last commit

#install SSL for GITHUB
'''
